# Backups von apply-rulesets.sh (Issue #7)
ruleset-backups/

# Secrets / sensible Dateien (dev-standards Pflichteinträge, Issue #69)
.env
.env.local
.env.*.local
credentials.json
*.jks
*.keystore
*.p12
*.p8

# Interne, nicht-öffentliche Docs (ausführliche Guides/Standards liegen im
# lokalen Projekt-Memory, nicht im öffentlichen Repo — Issue: README-Kürzung 2026-09-20)
docs/
dev-standards/global-policy.md
dev-standards/about-section.md

# Claude Code – nur maschinenspezifische Artefakte ignorieren.
# .claude/commands/ wird bewusst getrackt (Issue #31) → NICHT ignorieren.
.claude/settings.local.json
.claude/cache/

# PR Review & Merge Workflow – {{PROJECT_NAME}}

1. PR gegen `testing` validieren (nie direkt gegen `staging`/`main`)
2. CI + Security-Checks grün prüfen
3. Review-Kommentare abarbeiten und erneut testen
4. Erst nach expliziter Freigabe auf `main` mergen
5. Nach Merge nur Feature-Branch löschen

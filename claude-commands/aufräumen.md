# Tagesabschluss – {{PROJECT_NAME}}

1. `git status` prüfen (nur relevante Änderungen behalten)
2. Offene Feature-Branches prüfen und nur gemergte Feature-Branches löschen
3. Offene PRs + Review-Status prüfen
4. Letzte GitHub Actions Runs auf Fehler prüfen
5. Relevante offene Issues aktualisieren oder schließen
6. Kurznotiz im Changelog/Arbeitslog ergänzen
